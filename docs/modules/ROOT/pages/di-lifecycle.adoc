= DI & Lifecycle
include::partial$_attributes.adoc[]
:icons: font

Auto-scan determinístico via reflexão.

== AddDependencyWalk

[source,csharp]
----
// Program.cs — assemblies explícitos, determinístico
services.AddDependencyWalk("MeuProjeto", configuration, typeof(Program).Assembly);
services.AddDependencyWalk("MeuProjeto", configuration, typeof(Program).Assembly, typeof(UserRepository).Assembly);
----

[source,mermaid]
----
flowchart TD
  Scan["Assembly.GetTypes"] --> Filter["Where Has Injectable Service Repository"]
  Filter --> Lifetime["Map Attribute to Lifetime"]
  Lifetime --> Register["Add ServiceDescriptor"]
  Register --> Listeners["RegisterEventListeners"]
  Register --> Lifecycle["PostConstruct PreDestroy"]
----

== Atributos

[cols="1,1,2"]
|===
| Atributo | Lifetime padrão | Uso

| `[Injectable(Lifetime)]`
| obrigatório informar
| Qualquer classe que precise de DI

| `[Service]`
| Transient
| Serviços sem estado

| `[Repository]`
| Scoped
| Acesso a dados
|===

[source,csharp]
----
[Service]
public class UserService(IUserRepository repo) { }

[Repository]
public class UserRepository(IDbConnectionFactory f) { }

[Injectable(InjectableLifetime.Singleton)]
public class CacheManager { }

[Injectable(InjectableLifetime.Scoped)]
public class TenantScope { }
----

== ResolverExtensions

*   Cria via `ActivatorUtilities.CreateInstance` (resolve deps DI)
*   Injeta `[InjectConfig]` de `IConfiguration`
*   Executa `[PostConstruct]` (sync/async)
*   Registra `[EventListener]` no EventBus
*   `DiSmokeTests` 2 testes agnósticos `GetServices<IRepo>.Count==1 + ValidateScopes`

== Lifecycle — PostConstruct / PreDestroy

[source,csharp]
----
public class EmailService {
  private SmtpClient? _client;

  [PostConstruct]
  public void Initialize() => _client = new SmtpClient("smtp.example.com");

  [PostConstruct]
  public async Task InitializeAsync() => _client = await CreateClientAsync();

  [PreDestroy]
  public void Cleanup() => _client?.Dispose();
}
----

Suporta `void`, `Task`, `ValueTask` — múltiplos `[PostConstruct]` invalido (`InvalidOperationException`).

== Como funciona

O scan utiliza reflexão sobre assemblies explícitos, mapeando atributos `[Injectable]`, `[Service]` e `[Repository]` para `ServiceDescriptor`. Determinístico e auditável — sem source generators ou mágica.

*Filtro por namespace:* `ResolverExtensions` só registra interfaces cujo `Namespace` começa com `rootNamespace` (`"MeuProjeto"`), excluindo `IDisposable`/`IAsyncDisposable`. Classe sem interface vira `ServiceDescriptor` para o próprio tipo concreto. Ver `ResolverExtensions.cs:26`.

*Defaults:* `[Service]` → `Transient` (sem estado), `[Repository]` → `Scoped` (um por request/scope), `[Injectable]` → obrigatório passar `InjectableLifetime`.

*Conexão sem OpenAsync:* `IDbConnectionFactory.CreateConnection()` retorna `IDbConnection` (sync). Dapper chama `Open` automaticamente no `QueryAsync`/`ExecuteAsync`, por isso `using var conn = factory.CreateConnection(); await conn.QueryAsync(...)` funciona sem `OpenAsync`. Para transação use `UnitOfWork` do pack (`Footing.Framework.Data.UnitOfWork`), que abre via `DbConnection.OpenAsync` quando é `DbConnection` e faz `Rollback` no `DisposeAsync` se não houve `CommitAsync`.

[source,csharp]
----
await using var uow = new UnitOfWork(factory);
await uow.BeginAsync();
await uow.Connection.ExecuteAsync(sql, parms, uow.Transaction);
await uow.CommitAsync();
----
`UnitOfWork` está no pack (`Footing.Framework.Data`), não é exemplo para copiar.

== Config — InjectConfig

[source,csharp]
----
public class EmailSettings {
  [InjectConfig("Smtp:Host")]
  public string? Host { get; set; }

  [InjectConfig("Smtp:Port")]
  public int Port { get; set; }
}
// preenchido via IConfiguration automaticamente pelo ResolverExtensions
----

TIP: Para testes, use `ResolverExtensions` direto sem Host — ver `DiSmokeTests.cs`.

== Ver também

*   xref:eventbus.adoc[EventBus] — registro automático de handlers
*   xref:observability.adoc[OTel & Health] — `PostConstruct` sem deadlock
